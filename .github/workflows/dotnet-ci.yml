name: .NET Build & Test (auto-split by test class)

on:
  push:
    branches: ['**']          # run on all branches
  pull_request:
    branches: ['**']          # PRs to any branch
  workflow_dispatch: {}

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

env:
  SOLUTION: AzureBatchJobAutomation.sln
  DOTNET_VERSION: '8.0.x'

jobs:
  discover-tests:
    name: Discover test classes
    runs-on: ubuntu-latest
    outputs:
      matrix: ${{ steps.discover.outputs.matrix }}
      count:  ${{ steps.discover.outputs.count }}
    steps:
      - uses: actions/checkout@v4

      - name: Find *Tests.cs under /Tests and build matrix
        id: discover
        shell: pwsh
        run: |
          $root = Join-Path $PWD "Tests"
          if (-not (Test-Path $root)) {
            Write-Host "No 'Tests' folder found."
            $empty = @{ class = @() } | ConvertTo-Json -Compress
            "matrix=$empty" | Out-File -FilePath $env:GITHUB_OUTPUT -Append
            "count=0"       | Out-File -FilePath $env:GITHUB_OUTPUT -Append
            exit 0
          }

          $files = Get-ChildItem -Path $root -Filter '*Tests.cs' -Recurse -ErrorAction SilentlyContinue
          $classes = @()
          foreach ($f in $files) {
            $base = [System.IO.Path]::GetFileNameWithoutExtension($f.Name)  # e.g. BatchValidatorTests
            if ($classes -notcontains $base) { $classes += $base }
          }

          $matrixObj = @{ class = $classes }
          $matrixJson = $matrixObj | ConvertTo-Json -Compress
          Write-Host "Discovered classes: $($classes -join ', ')"
          "matrix=$matrixJson" | Out-File -FilePath $env:GITHUB_OUTPUT -Append
          "count=$($classes.Count)" | Out-File -FilePath $env:GITHUB_OUTPUT -Append

  tests:
    name: Tests • ${{ matrix.class }}
    needs: discover-tests
    if: ${{ needs.discover-tests.outputs.count != '0' }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix: ${{ fromJSON(needs.discover-tests.outputs.matrix) }}
    env:
      CLASS: ${{ matrix.class }}
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: ${{ env.DOTNET_VERSION }}

      - name: Restore
        run: dotnet restore "${{ env.SOLUTION }}"

      - name: Build (Release)
        run: dotnet build "${{ env.SOLUTION }}" --no-restore --configuration Release

      # Light assertion so you get a clear error if the filter matches nothing
      - name: Ensure class exists in test list
        shell: pwsh
        run: |
          $list = dotnet test "${{ env.SOLUTION }}" --no-build --list-tests
          if ($list -notmatch ("\b{0}\b" -f $env:CLASS)) {
            throw "No tests found for class '$env:CLASS'."
          }

      - name: Run tests (ClassName=${{ env.CLASS }})
        run: |
          dotnet test "${{ env.SOLUTION }}" \
            --no-build --configuration Release \
            --filter "ClassName=${{ env.CLASS }}" \
            --logger "trx" \
            --collect "XPlat Code Coverage"

      - name: Collect TRX & coverage artifacts
        shell: bash
        run: |
          mkdir -p artifacts/testresults artifacts/coverage
          find . -type f -path "*/TestResults/*.trx" -exec cp {} artifacts/testresults/ \; || true
          find . -type f -path "*/TestResults/*/coverage.cobertura.xml" -exec cp {} artifacts/coverage/ \; || true
          # Always leave something so the artifact exists
          if [ -z "$(ls -A artifacts/testresults 2>/dev/null)" ]; then
            echo "No TRX produced for ${CLASS}" > artifacts/testresults/no-tests.txt
          fi

      - name: Upload TRX
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: test-results-${{ matrix.class }}
          path: artifacts/testresults
          if-no-files-found: warn

      - name: Upload Coverage
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: coverage-${{ matrix.class }}
          path: artifacts/coverage
          if-no-files-found: warn

  summarize:
    name: Summary (Passed / Failed / Skipped)
    runs-on: ubuntu-latest
    needs: tests
    steps:
      - name: Download TRX artifacts
        uses: actions/download-artifact@v4
        with:
          pattern: test-results-*
          merge-multiple: true
          path: downloads/testresults

      # Nice summary (table + PR check) from TRX files
      - name: Publish unit test summary
        uses: EnricoMi/publish-unit-test-result-action@v2
        with:
          files: downloads/testresults/**/*.trx
          check_name: ".NET Tests Summary"
          comment_mode: off
          fail_on: nothing

      # Quick totals in the run summary
      - name: Totals (Markdown)
        shell: pwsh
        run: |
          $trxFiles = Get-ChildItem -Path "downloads/testresults" -Recurse -Filter *.trx -ErrorAction SilentlyContinue
          if (-not $trxFiles) {
            "## Test Summary`nNo TRX files found." | Out-File -FilePath $env:GITHUB_STEP_SUMMARY
            exit 0
          }
          function N($v){ if ($null -eq $v -or $v -eq "") {0} else {[int]$v} }
          $T=0;$P=0;$F=0;$S=0
          foreach($f in $trxFiles){
            [xml]$x = Get-Content $f.FullName
            $c = $x.TestRun.ResultSummary.Counters
            $T += N $c.total
            $P += N $c.passed
            $F += (N $c.failed) + (N $c.error)
            $S += (N $c.notExecuted) + (N $c.inconclusive) + (N $c.notRunnable) + (N $c.pending) + (N $c.aborted) + (N $c.timeout)
          }
          "## Totals`n- Total: $T`n- Passed: $P`n- Failed: $F`n- Skipped: $S" | Out-File -FilePath $env:GITHUB_STEP_SUMMARY
